import{DashboardShell}from"@/components/dashboard/Shell";import{requireProfile}from"@/lib/auth/session";import{getEffectivePermissions}from"@/lib/auth/permissions";import{createSupabaseAdminClient}from"@/lib/supabase/admin";import{confirmOwnWorkplaceAction}from"./actions";export const dynamic="force-dynamic";export default async function EmployeeProfilePage({searchParams}:{searchParams:Promise<{success?:string;error?:string}>}){const{profile}=await requireProfile(["employee"]);const db=createSupabaseAdminClient();const[permissions,message,{data:locations},{data:assignment}]=await Promise.all([getEffectivePermissions(profile.id),searchParams,db.from("work_locations").select("id,name,code,location_type,city,country_code").eq("is_active",true).order("name"),db.from("profile_work_locations").select("work_location_id,work_locations(name,code,city,country_code)").eq("profile_id",profile.id).eq("is_primary",true).eq("is_active",true).maybeSingle()]);const current=assignment?.work_locations as unknown as {name:string;code:string;city:string|null;country_code:string}|null;return <DashboardShell title="My workplace" subtitle="Confirm the approved workplace used for event-based inventory location snapshots." employeePermissions={permissions}>{message.success?<p className="mb-4 rounded border border-green-200 bg-green-50 p-3">{message.success}</p>:null}{message.error?<p className="mb-4 rounded border border-red-200 bg-red-50 p-3">{message.error}</p>:null}<section className="rounded-xl border bg-[var(--surface)] p-6"><h2 className="text-xl font-semibold">Current primary workplace</h2><p className="mt-3">{current?`${current.name} (${current.code}) · ${current.city??"—"}, ${current.country_code}`:"No verified workplace is assigned. Physical serialized-inventory operations remain blocked."}</p><form action={confirmOwnWorkplaceAction} className="mt-5 flex flex-col gap-3 sm:flex-row"><select name="work_location_id" required defaultValue={assignment?.work_location_id??""} className="min-h-12 flex-1 rounded border p-3"><option value="">Choose an approved location</option>{(locations??[]).map((item)=><option key={item.id} value={item.id}>{item.name} ({item.code}) · {item.city??"—"}, {item.country_code}</option>)}</select><button className="rounded border px-5 py-3 font-semibold">Confirm workplace</button></form><p className="mt-4 text-sm text-[var(--muted-text)]">This is a workplace snapshot, not continuous GPS. An administrator can correct assignments when needed.</p></section></DashboardShell>}
