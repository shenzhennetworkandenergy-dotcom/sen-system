import { DashboardShell } from "@/components/dashboard/Shell";
import { requireEmployee } from "@/lib/auth/server";
const nav = ["Profile","Future Permissions","CRM","Sales","Inventory","Support"].map((label, i) => ({ label, href: "/employee", soon: i > 0 }));
export default async function EmployeePage() { const profile = await requireEmployee(); return <DashboardShell title="Employee workspace" profile={profile} nav={nav}><section className="rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-6"><h2 className="text-xl font-bold">Employee profile</h2><dl className="mt-4 grid gap-3 sm:grid-cols-2"><div><dt className="text-sm text-[var(--muted-text)]">Name</dt><dd>{profile.full_name}</dd></div><div><dt className="text-sm text-[var(--muted-text)]">Email</dt><dd>{profile.email}</dd></div><div><dt className="text-sm text-[var(--muted-text)]">Role</dt><dd>{profile.role}</dd></div><div><dt className="text-sm text-[var(--muted-text)]">Status</dt><dd>{profile.status}</dd></div></dl><p className="mt-4 text-[var(--muted-text)]">Granular ERP module permissions and activity reporting are reserved for Phase 3B.</p></section></DashboardShell>; }
