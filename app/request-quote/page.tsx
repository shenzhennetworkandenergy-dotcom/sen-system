import { siteConfig } from "@/config/site";import { Button } from "@/components/ui/Button";import { Section } from "@/components/ui/Section";
export default function RequestQuotePage(){return <Section><div className="max-w-3xl"><p className="text-sm font-semibold uppercase tracking-[0.18em] text-[var(--primary)]">Request quote</p><h1 className="mt-3 text-4xl font-semibold">Request a sourcing quotation</h1><p className="mt-4 text-lg leading-8 text-[var(--muted-text)]">Share your requirement directly by phone or WhatsApp. SEN has not enabled a web form submission workflow yet, so no fake form is shown.</p><div className="mt-8 flex flex-wrap gap-3"><Button href={siteConfig.contact.telephoneLink}>Call {siteConfig.contact.phoneDisplay}</Button><a href={siteConfig.contact.whatsappLink} target="_blank" rel="noopener noreferrer" className="inline-flex items-center rounded-[var(--radius-md)] bg-[var(--accent)] px-5 py-3 text-sm font-semibold text-white">Continue on WhatsApp</a><Button href={siteConfig.routes.contact} variant="outline">Office details</Button></div></div></Section>}
