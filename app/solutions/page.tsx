import { Section } from "@/components/ui/Section";import { siteConfig } from "@/config/site";export default function SolutionsPage(){return <Section><h1 className="text-4xl font-semibold">Solutions</h1><p className="mt-4 text-[var(--muted-text)]">Enterprise network, data center, energy, medical and industrial sourcing solutions.</p><div className="mt-8 grid gap-4 md:grid-cols-2">{siteConfig.businessCategories.map((c)=><a className="rounded-[var(--radius-lg)] border border-[var(--border)] p-5 font-semibold" href={c.href} key={c.slug}>{c.label}</a>)}</div></Section>}
