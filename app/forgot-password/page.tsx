import Link from "next/link";
import Image from "next/image";
import { senContact } from "@/lib/data/contact";
export default function ForgotPasswordPage() { return <main className="flex min-h-screen items-center justify-center p-6"><section className="max-w-xl rounded-2xl border border-[var(--border)] p-8"><Image src="/brand/sen-logo.svg" alt="SEN — Shenzhen Energy & Networks" width={180} height={76}/><h1 className="mt-6 text-3xl font-bold">Password help</h1><p className="mt-3 text-[var(--muted-text)]">Automated password recovery is not enabled yet. Contact SEN administration to verify your account and receive recovery guidance. Never share your current password with anyone.</p><div className="mt-5 rounded-xl bg-[var(--muted-surface)] p-4"><p className="font-semibold">{senContact.company}</p>{senContact.addressLines.map(line => <p key={line}>{line}</p>)}</div><div className="mt-5 flex gap-3"><a className="rounded-lg bg-[var(--primary)] px-4 py-2 text-[var(--primary-foreground)]" href={senContact.phoneHref}>Call {senContact.phoneDisplay}</a><a className="rounded-lg border border-[var(--border)] px-4 py-2" href={senContact.whatsappHref}>WhatsApp</a></div><Link className="mt-5 inline-block text-sm font-semibold" href="/login">Back to login</Link></section></main>; }
