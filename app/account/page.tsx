import { requireProfile } from "@/lib/auth/session";
import { DashboardShell } from "@/components/dashboard/Shell";
const future = ["Addresses", "Orders", "Quotations", "Wishlist", "Support", "Warranty / Serial"];
export default async function AccountPage(){ const { profile } = await requireProfile(["customer", "admin"]); return <DashboardShell title="My Account" subtitle="Manage your SEN profile, account status and future customer services."><section className="grid gap-6 lg:grid-cols-3"><div className="rounded-xl border bg-[var(--surface)] p-6 lg:col-span-2"><h2 className="text-xl font-semibold">Profile</h2><div className="mt-4 grid gap-4 md:grid-cols-2"><p><b>Name:</b> {profile.full_name ?? "Not provided"}</p><p><b>Email:</b> {profile.email}</p><p><b>Phone:</b> {profile.phone ?? "Not provided"}</p><p><b>Country:</b> {profile.country ?? "Bangladesh"}</p><p><b>Customer type:</b> {profile.customer_type ?? "individual"}</p><p><b>Company:</b> {profile.company_name ?? "—"}</p></div></div><div className="rounded-xl border bg-[var(--surface)] p-6"><h2 className="text-xl font-semibold">Account overview</h2><p className="mt-4"><b>Role:</b> {profile.role}</p><p><b>Status:</b> {profile.status}</p></div></section><section className="mt-6 grid gap-4 md:grid-cols-3">{future.map((item)=><div key={item} className="rounded-xl border bg-[var(--surface)] p-5"><h3 className="font-semibold">{item}</h3><p className="mt-2 text-sm text-[var(--muted-text)]">Future Phase 3 customer module placeholder.</p></div>)}</section></DashboardShell> }
