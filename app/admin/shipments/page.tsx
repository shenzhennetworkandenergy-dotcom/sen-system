import { connection } from "next/server";
import { DashboardShell } from "@/components/dashboard/Shell";
import { requirePermission } from "@/lib/auth/permissions";
import { getShipments } from "@/lib/orders/data";
import { dateTime, label } from "@/lib/orders/types";
export const dynamic = "force-dynamic";
export default async function ShipmentsPage({ searchParams }: { searchParams: Promise<{ q?: string; status?: string; mode?: string; page?: string; error?: string }> }) { await connection(); await requirePermission("shipments.view"); const params = await searchParams, { shipments, count, page, size } = await getShipments(params); return <DashboardShell admin title="Shipments" subtitle="Manage partial dispatches, tracking events, locations and customer-visible delivery progress.">{params.error ? <p className="mb-5 rounded-xl border border-red-200 bg-red-50 p-4 text-red-900">{params.error}</p> : null}<form className="mb-5 grid gap-3 rounded-2xl border bg-[var(--surface)] p-4 md:grid-cols-[1fr_12rem_12rem_auto]"><input name="q" defaultValue={params.q} placeholder="Shipment or external reference" className="rounded-xl border px-4 py-2.5"/><select name="status" defaultValue={params.status} className="rounded-xl border px-4 py-2.5"><option value="">All statuses</option>{["draft","confirmed","ready","dispatched","in_transit","arrived","out_for_delivery","delivered","cancelled"].map((value) => <option key={value} value={value}>{label(value)}</option>)}</select><select name="mode" defaultValue={params.mode} className="rounded-xl border px-4 py-2.5"><option value="">All modes</option>{["air","sea","road","local_delivery","customer_pickup","other"].map((value) => <option key={value} value={value}>{label(value)}</option>)}</select><button className="rounded-xl bg-[var(--primary)] px-4 py-2.5 font-semibold text-[var(--primary-foreground)]">Filter</button></form><div className="grid gap-4 xl:grid-cols-2">{shipments.map((shipment) => { const order = shipment.sales_orders as unknown as { order_number: string; profiles: { full_name: string | null; email: string } | null } | null; return <article key={shipment.id} className="rounded-2xl border bg-[var(--surface)] p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><a href={`/admin/shipments/${shipment.id}`} className="text-lg font-semibold text-[var(--primary)]">{shipment.shipment_number}</a><p className="text-sm text-[var(--muted-text)]">Order {order?.order_number} · {order?.profiles?.full_name || order?.profiles?.email}</p></div><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-800">{label(shipment.status)}</span></div><dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-[var(--muted-text)]">Mode</dt><dd className="font-semibold">{label(shipment.transport_mode)}</dd></div><div><dt className="text-[var(--muted-text)]">Created</dt><dd>{dateTime(shipment.created_at)}</dd></div><div><dt className="text-[var(--muted-text)]">Estimated arrival</dt><dd>{dateTime(shipment.estimated_arrival_at)}</dd></div><div><dt className="text-[var(--muted-text)]">Packages</dt><dd>{shipment.package_count}</dd></div></dl></article>; })}</div>{!shipments.length ? <p className="rounded-2xl border bg-[var(--surface)] p-10 text-center text-[var(--muted-text)]">No shipments match these filters.</p> : null}<div className="mt-4 flex justify-between text-sm"><span>{count} shipment(s)</span><div className="flex gap-2">{page > 1 ? <a href={`?page=${page - 1}`} className="rounded border px-3 py-1">Previous</a> : null}{page * size < count ? <a href={`?page=${page + 1}`} className="rounded border px-3 py-1">Next</a> : null}</div></div></DashboardShell>; }
