import { DashboardShell } from "@/components/dashboard/Shell";import { requireAnyPermission } from "@/lib/auth/permissions";import { getInventorySelectors } from "@/lib/inventory/data";import { adjustInventoryAction,transferInventoryAction } from "../../actions";
export default async function NewAdjustmentPage({searchParams}:{searchParams:Promise<{success?:string;error?:string}>}){const{profile,permissions}=await requireAnyPermission(["inventory.adjust_stock","inventory.transfer"]);const[options,message]=await Promise.all([getInventorySelectors(),searchParams]);const productOptions=options.products.map(p=><option key={p.id} value={p.id}>{p.name} · {p.sku}{p.serial_tracking_required?" · serialized":""}</option>);return <DashboardShell admin={profile.role==="admin"} employeePermissions={profile.role==="employee"?permissions:undefined} title="Stock operations" subtitle="Create an atomic adjustment, opening balance, or confirmed warehouse transfer.">{message.error?<p className="mb-4 rounded border border-red-200 bg-red-50 p-3 text-red-900">{message.error}</p>:null}<div className="grid gap-6 xl:grid-cols-2"><form action={adjustInventoryAction} className="space-y-4 rounded-xl border bg-[var(--surface)] p-6"><h2 className="text-xl font-semibold">Adjust stock</h2><label>Warehouse<select name="warehouse_id" required className="mt-1 w-full rounded border p-3"><option value="">Select warehouse</option>{options.warehouses.map(x=><option key={x.id} value={x.id}>{x.name} ({x.code})</option>)}</select></label><label>Product<select name="product_id" required className="mt-1 w-full rounded border p-3"><option value="">Select product</option>{productOptions}</select></label><label>Variation (optional)<select name="variation_id" className="mt-1 w-full rounded border p-3"><option value="">Parent product</option>{options.variations.map(x=><option key={x.id} value={x.id}>{x.sku} · {x.combination_key}</option>)}</select></label><label>Reason<select name="reason_id" required className="mt-1 w-full rounded border p-3"><option value="">Select reason</option>{options.reasons.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Quantity change<input name="quantity_change" type="number" step="0.0001" required placeholder="Use a negative value to decrease" className="mt-1 w-full rounded border p-3"/></label><label>Serial numbers<textarea name="serials" placeholder="One serial per line when serial tracking is enabled" className="mt-1 min-h-28 w-full rounded border p-3"/></label><label>Notes<textarea name="notes" className="mt-1 min-h-24 w-full rounded border p-3"/></label><button className="min-h-12 rounded bg-[var(--primary)] px-5 py-3 font-semibold text-[var(--primary-foreground)]">Confirm adjustment</button></form><form action={transferInventoryAction} className="space-y-4 rounded-xl border bg-[var(--surface)] p-6"><h2 className="text-xl font-semibold">Transfer stock</h2><label>Source warehouse<select name="source_id" required className="mt-1 w-full rounded border p-3"><option value="">Select source</option>{options.warehouses.map(x=><option key={x.id} value={x.id}>{x.name} ({x.code})</option>)}</select></label><label>Destination warehouse<select name="destination_id" required className="mt-1 w-full rounded border p-3"><option value="">Select destination</option>{options.warehouses.map(x=><option key={x.id} value={x.id}>{x.name} ({x.code})</option>)}</select></label><label>Product<select name="product_id" required className="mt-1 w-full rounded border p-3"><option value="">Select product</option>{productOptions}</select></label><label>Variation (optional)<select name="variation_id" className="mt-1 w-full rounded border p-3"><option value="">Parent product</option>{options.variations.map(x=><option key={x.id} value={x.id}>{x.sku} · {x.combination_key}</option>)}</select></label><label>Quantity<input name="quantity" type="number" min="0.0001" step="0.0001" required className="mt-1 w-full rounded border p-3"/></label><label>Serial numbers<textarea name="serials" placeholder="One serial per line for serialized products" className="mt-1 min-h-28 w-full rounded border p-3"/></label><label>Notes<textarea name="notes" className="mt-1 min-h-24 w-full rounded border p-3"/></label><button className="min-h-12 rounded bg-[var(--primary)] px-5 py-3 font-semibold text-[var(--primary-foreground)]">Confirm transfer</button></form></div></DashboardShell>}
