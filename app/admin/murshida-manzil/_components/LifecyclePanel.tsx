import { createTenantAction, createUnitAction, deleteTenantAction, deleteUnitAction, setTenantActiveAction, setUnitActiveAction, updateTenantAction, updateUnitAction } from "@/app/admin/murshida-manzil/actions";

export function LifecyclePanel({ units, tenants }: { units: Array<{ id: string; unit_code: string; is_active: boolean }>; tenants: Array<{ id: string; name: string; phone_number?: string | null; unit_id: string | null; monthly_rent: number; is_active: boolean; units?: unknown }> }) {
  const activeUnits = units.filter((unit) => unit.is_active);
  const inactiveUnits = units.filter((unit) => !unit.is_active);
  const activeTenants = tenants.filter((tenant) => tenant.is_active);
  const inactiveTenants = tenants.filter((tenant) => !tenant.is_active);
  const unitName = (id: string | null) => units.find((unit) => unit.id === id)?.unit_code ?? "—";
  return <div className="mb-4 grid gap-4 xl:grid-cols-2"><section className="rounded-xl border bg-[var(--surface)] p-5 shadow-sm"><h2 className="text-lg font-bold">Units</h2><form action={createUnitAction} className="mt-3 grid gap-2 sm:grid-cols-[1fr_auto_auto]"><input name="unit_code" required placeholder="Unit identifier" className="rounded-lg border px-3 py-2" /><label className="flex items-center gap-1 text-sm"><input name="is_active" type="checkbox" defaultChecked /> Active</label><button className="rounded-lg bg-[var(--primary)] px-3 py-2 font-semibold text-[var(--primary-foreground)]">Add Unit</button></form><details className="mt-3"><summary className="inline-flex cursor-pointer rounded border px-3 py-1.5 text-sm font-semibold">See All Units</summary><div className="mt-3 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-2">Unit</th><th className="p-2">Current Tenant</th><th className="p-2">Status</th><th className="p-2">Actions</th></tr></thead><tbody>{activeUnits.map((unit) => <tr key={unit.id} className="border-b"><td className="p-2">{unit.unit_code}</td><td className="p-2">{activeTenants.find((tenant) => tenant.unit_id === unit.id)?.name ?? "—"}</td><td className="p-2">Active</td><td className="p-2"><details><summary className="cursor-pointer underline">Edit</summary><form action={updateUnitAction.bind(null, unit.id)} className="mt-2 grid gap-2"><input name="unit_code" required defaultValue={unit.unit_code} className="rounded border px-2 py-1.5" /><label><input name="is_active" type="checkbox" defaultChecked /> Active</label><button className="rounded border px-3 py-1.5">Save</button></form></details><form action={deleteUnitAction.bind(null, unit.id)}><button className="mt-1 text-sm text-red-700 underline">Delete / Archive</button></form></td></tr>)}</tbody></table></div></details><details className="mt-3"><summary className="inline-flex cursor-pointer rounded border px-3 py-1.5 text-sm font-semibold">Archived / Inactive Units</summary><div className="mt-3 overflow-x-auto"><table className="w-full text-left text-sm"><tbody>{inactiveUnits.map((unit) => <tr key={unit.id} className="border-b"><td className="p-2">{unit.unit_code}</td><td className="p-2">{tenants.find((tenant) => tenant.unit_id === unit.id)?.name ?? "—"}</td><td className="p-2">Inactive / Archived</td><td className="p-2"><form action={setUnitActiveAction.bind(null, unit.id, true)}><button className="underline">Restore</button></form></td></tr>)}</tbody></table></div></details></section><section className="rounded-xl border bg-[var(--surface)] p-5 shadow-sm"><h2 className="text-lg font-bold">Tenants</h2><form action={createTenantAction} className="mt-3 grid gap-2 md:grid-cols-5"><input name="name" required placeholder="Tenant name" className="rounded-lg border px-3 py-2" /><input name="phone_number" placeholder="Mobile number" className="rounded-lg border px-3 py-2" /><select name="unit_id" required className="rounded-lg border px-3 py-2"><option value="">Select active unit</option>{activeUnits.map((unit) => <option key={unit.id} value={unit.id}>{unit.unit_code}</option>)}</select><input name="monthly_rent" required type="number" min="0.01" step="0.01" placeholder="Monthly rent" className="rounded-lg border px-3 py-2" /><label className="flex items-center gap-1 text-sm"><input name="is_active" type="checkbox" defaultChecked /> Active</label><button className="rounded-lg bg-[var(--primary)] px-3 py-2 font-semibold text-[var(--primary-foreground)]">Add Tenant</button></form><details className="mt-3"><summary className="inline-flex cursor-pointer rounded border px-3 py-1.5 text-sm font-semibold">See All Tenants</summary><div className="mt-3 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-2">Tenant Name</th><th className="p-2">Mobile</th><th className="p-2">Unit</th><th className="p-2">Monthly Rent</th><th className="p-2">Status</th><th className="p-2">Actions</th></tr></thead><tbody>{activeTenants.map((tenant) => <tr key={tenant.id} className="border-b"><td className="p-2">{tenant.name}</td><td className="p-2">{tenant.phone_number ?? "—"}</td><td className="p-2">{unitName(tenant.unit_id)}</td><td className="p-2">{tenant.monthly_rent}</td><td className="p-2">Active</td><td className="p-2"><details><summary className="cursor-pointer underline">Edit</summary><form action={updateTenantAction.bind(null, tenant.id)} className="mt-2 grid gap-2"><input name="name" required defaultValue={tenant.name} className="rounded border px-2 py-1.5" /><input name="phone_number" defaultValue={tenant.phone_number ?? ""} className="rounded border px-2 py-1.5" /><select name="unit_id" required defaultValue={tenant.unit_id ?? ""} className="rounded border px-2 py-1.5">{activeUnits.map((unit) => <option key={unit.id} value={unit.id}>{unit.unit_code}</option>)}</select><input name="monthly_rent" required type="number" min="0.01" step="0.01" defaultValue={tenant.monthly_rent} className="rounded border px-2 py-1.5" /><label><input name="is_active" type="checkbox" defaultChecked /> Active</label><button className="rounded border px-3 py-1.5">Save</button></form></details><form action={deleteTenantAction.bind(null, tenant.id)}><button className="mt-1 text-sm text-red-700 underline">Delete / Archive</button></form></td></tr>)}</tbody></table></div></details><details className="mt-3"><summary className="inline-flex cursor-pointer rounded border px-3 py-1.5 text-sm font-semibold">Archived / Inactive Tenants</summary><div className="mt-3 overflow-x-auto"><table className="w-full text-left text-sm"><tbody>{inactiveTenants.map((tenant) => <tr key={tenant.id} className="border-b"><td className="p-2">{tenant.name}</td><td className="p-2">{tenant.phone_number ?? "—"}</td><td className="p-2">{unitName(tenant.unit_id)}</td><td className="p-2">{tenant.monthly_rent}</td><td className="p-2">Inactive / Archived</td><td className="p-2"><form action={setTenantActiveAction.bind(null, tenant.id, true)}><button className="underline">Restore</button></form></td></tr>)}</tbody></table></div></details></section></div>;
}
