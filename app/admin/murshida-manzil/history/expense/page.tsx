import { requireProfile } from "@/lib/auth/session";
import { listExpenseHistory } from "@/lib/murshida-manzil/repository";
export const dynamic = "force-dynamic";
export default async function ExpenseHistoryPage() { await requireProfile(["admin"]); const rows = await listExpenseHistory(); return <main className="mx-auto max-w-6xl p-8"><h1 className="text-2xl font-bold">MURSHIDA MANZIL · Expense History</h1><div className="mt-6 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-2">Voucher</th><th className="p-2">Date</th><th className="p-2">Description</th><th className="p-2">Category</th><th className="p-2">Paid To</th><th className="p-2">Amount</th><th className="p-2">Payment Method</th><th className="p-2">Action</th></tr></thead><tbody>{rows.map((row) => <tr key={String(row.id)} className="border-b"><td className="p-2">{String(row.voucher_number ?? `Expense-${String(row.id).slice(0, 8)}`)}</td><td className="p-2">{String(row.expense_date)}</td><td className="p-2">{String(row.description)}</td><td className="p-2">{String(row.category ?? "—")}</td><td className="p-2">{String(row.paid_to ?? "—")}</td><td className="p-2">{String(row.amount)}</td><td className="p-2">{String(row.payment_method ?? "—")}</td><td className="p-2"><a className="underline" href={`/admin/murshida-manzil/${row.id}/expense-voucher`}>View / Print Voucher</a></td></tr>)}</tbody></table></div></main>; }
