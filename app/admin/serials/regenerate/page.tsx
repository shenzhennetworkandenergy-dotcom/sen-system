import{DashboardShell}from"@/components/dashboard/Shell";import{requirePermission}from"@/lib/auth/permissions";import{createSupabaseAdminClient}from"@/lib/supabase/admin";import{regenerateSerialAction}from"../actions";
export const dynamic="force-dynamic";
export default async function RegenerateSerialsPage(){const{profile,permissions}=await requirePermission("serials.regenerate"),db=createSupabaseAdminClient();const{data:serials}=await db.from("serial_numbers").select("id,sen_serial,status,created_at").in("status",["expected","received","available"]).order("created_at",{ascending:false}).limit(100);return <DashboardShell admin={profile.role==="admin"} employeePermissions={profile.role==="employee"?permissions:undefined} title="Regenerate eligible serials" subtitle="Only unallocated, unshipped physical units can receive a replacement identifier.">{serials?.length?<div className="grid gap-3">{serials.map(s=><form action={regenerateSerialAction} key={s.id} className="grid gap-3 rounded-xl border bg-[var(--surface)] p-4 md:grid-cols-[1fr_1fr_auto]"><input type="hidden" name="serial_id" value={s.id}/><div><b className="font-mono text-sm">{s.sen_serial}</b><span className="block text-sm text-[var(--muted-text)]">{s.status}</span></div><input name="reason" required minLength={5} placeholder="Required regeneration reason" className="rounded border p-2"/><button className="rounded border px-4 py-2 font-semibold">Regenerate</button></form>)}</div>:<p className="rounded-xl border bg-[var(--surface)] p-8">No serials are currently eligible for regeneration.</p>}</DashboardShell>}
