"use server";
import { revalidatePath } from "next/cache"; import { redirect } from "next/navigation"; import { requirePermission } from "@/lib/auth/permissions"; import { createSupabaseAdminClient } from "@/lib/supabase/admin"; import { writeAuditLog } from "@/lib/audit/log"; import { checked,optionalNumber,optionalText,productStatuses,productTypes,requiredText,slugify,uuidOrNull,businessCategories } from "@/lib/inventory/validation";
function target(id?:string,type:"success"|"error"="success",message="Saved"){return id?`/admin/products/${id}?${type}=${encodeURIComponent(message)}`:`/admin/products?${type}=${encodeURIComponent(message)}`;}
function payload(form:FormData){const name=requiredText(form,"name"),sku=requiredText(form,"sku",100),product_type=String(form.get("product_type")),status=String(form.get("status")),sen_business_category=String(form.get("sen_business_category")),regular_price=optionalNumber(form,"regular_price"),sale_price=optionalNumber(form,"sale_price");if(!productTypes.includes(product_type as never)||!productStatuses.includes(status as never)||!businessCategories.includes(sen_business_category as never))throw new Error("Invalid product type, status, or business category.");if(sale_price!==null&&regular_price!==null&&sale_price>regular_price)throw new Error("Sale price cannot exceed regular price.");let specifications={};try{specifications=JSON.parse(String(form.get("specifications")??"{}"));}catch{throw new Error("Specifications must be valid JSON.");}return{name,sku,slug:slugify(String(form.get("slug")||name)),product_type,status,sen_business_category,brand_id:uuidOrNull(form.get("brand_id")),barcode:optionalText(form,"barcode",100),manufacturer_part_number:optionalText(form,"manufacturer_part_number",100),short_description:optionalText(form,"short_description",1000),description:optionalText(form,"description",10000),specifications,internal_notes:optionalText(form,"internal_notes",5000),warranty_information:optionalText(form,"warranty_information",1000),purchase_cost:optionalNumber(form,"purchase_cost"),regular_price,sale_price,currency:requiredText(form,"currency",3).toUpperCase(),weight:optionalNumber(form,"weight"),length:optionalNumber(form,"length"),width:optionalNumber(form,"width"),height:optionalNumber(form,"height"),country_of_origin:optionalText(form,"country_of_origin",100),manage_stock:checked(form,"manage_stock"),stock_status:String(form.get("stock_status")??"in_stock"),low_stock_threshold:optionalNumber(form,"low_stock_threshold")??0,allow_backorders:checked(form,"allow_backorders"),sold_individually:checked(form,"sold_individually"),serial_tracking_required:checked(form,"serial_tracking_required"),batch_tracking_enabled:checked(form,"batch_tracking_enabled"),featured:checked(form,"featured"),public_catalogue_visible:checked(form,"public_catalogue_visible")};}
async function setCategory(productId:string,categoryId:string|null){const db=createSupabaseAdminClient();await db.from("product_category_assignments").delete().eq("product_id",productId);if(categoryId)await db.from("product_category_assignments").insert({product_id:productId,category_id:categoryId,is_primary:true});}
export async function createProductAction(form:FormData){const{profile}=await requirePermission("products.create");try{const db=createSupabaseAdminClient(),data={...payload(form),created_by:profile.id,updated_by:profile.id};const{data:created,error}=await db.from("products").insert(data).select("id").single();if(error||!created)throw new Error(error?.message??"Create failed");await setCategory(created.id,uuidOrNull(form.get("category_id")));await writeAuditLog({actorId:profile.id,actorRole:profile.role,action:"product.created",module:"products",entityType:"product",entityId:created.id,description:"Product created.",newValues:{name:data.name,sku:data.sku,product_type:data.product_type,status:data.status}});revalidatePath("/admin/products");redirect(target(created.id,"success","Product created."));}catch(error){console.error("Product create failed",{message:error instanceof Error?error.message:"Unknown"});redirect(target(undefined,"error",error instanceof Error&&/required|Invalid|price|JSON/i.test(error.message)?error.message:"Unable to create product."));}}
export async function updateProductAction(productId:string,form:FormData){const{profile}=await requirePermission("products.edit");try{const db=createSupabaseAdminClient();const{data:old}=await db.from("products").select("name,sku,status,product_type").eq("id",productId).maybeSingle();if(!old)throw new Error("Product not found");const data={...payload(form),updated_by:profile.id,updated_at:new Date().toISOString()};const{error}=await db.from("products").update(data).eq("id",productId);if(error)throw new Error(error.message);await setCategory(productId,uuidOrNull(form.get("category_id")));await writeAuditLog({actorId:profile.id,actorRole:profile.role,action:"product.updated",module:"products",entityType:"product",entityId:productId,description:"Product updated.",oldValues:old,newValues:{name:data.name,sku:data.sku,status:data.status,product_type:data.product_type}});revalidatePath(`/admin/products/${productId}`);revalidatePath("/admin/products");redirect(target(productId,"success","Product updated."));}catch(error){console.error("Product update failed",{message:error instanceof Error?error.message:"Unknown"});redirect(target(productId,"error",error instanceof Error&&/required|Invalid|price|JSON|not found/i.test(error.message)?error.message:"Unable to update product."));}}
export async function archiveProductAction(form:FormData){const{profile}=await requirePermission("products.archive");const ids=[...new Set(form.getAll("productIds").map(String))].filter(x=>/^[0-9a-f-]{36}$/i.test(x)).slice(0,100);if(!ids.length)redirect(target(undefined,"error","Select at least one product."));const db=createSupabaseAdminClient();const{error}=await db.from("products").update({status:"archived",updated_by:profile.id,updated_at:new Date().toISOString()}).in("id",ids);if(error)redirect(target(undefined,"error","Unable to archive products."));await writeAuditLog({actorId:profile.id,actorRole:profile.role,action:"product.archived",module:"products",entityType:"product",description:"Products archived.",newValues:{product_ids:ids,count:ids.length}});revalidatePath("/admin/products");redirect(target(undefined,"success",`${ids.length} product(s) archived.`));}
export async function createVariationAction(productId:string,form:FormData){const{profile}=await requirePermission("products.edit");const db=createSupabaseAdminClient();const data={product_id:productId,sku:requiredText(form,"sku",100),combination_key:requiredText(form,"combination_key",500),regular_price:optionalNumber(form,"regular_price"),purchase_cost:optionalNumber(form,"purchase_cost"),manage_stock:checked(form,"manage_stock"),low_stock_threshold:optionalNumber(form,"low_stock_threshold")??0};const{data:created,error}=await db.from("product_variations").insert(data).select("id").single();if(error||!created)redirect(target(productId,"error","Unable to create variation. Check SKU, combination, and parent stock settings."));await writeAuditLog({actorId:profile.id,actorRole:profile.role,action:"product.variation_created",module:"products",entityType:"product_variation",entityId:created.id,description:"Product variation created.",newValues:{product_id:productId,sku:data.sku,combination_key:data.combination_key}});revalidatePath(`/admin/products/${productId}`);redirect(target(productId,"success","Variation created."));}
export async function uploadProductMediaAction(productId:string,form:FormData){const{profile}=await requirePermission("products.edit");const file=form.get("file");if(!(file instanceof File)||file.size===0||file.size>10485760||!["image/jpeg","image/png","image/webp","application/pdf"].includes(file.type))redirect(target(productId,"error","Choose a JPG, PNG, WebP, or PDF up to 10 MB."));const ext={"image/jpeg":"jpg","image/png":"png","image/webp":"webp","application/pdf":"pdf"}[file.type]!;const path=`${productId}/${crypto.randomUUID()}.${ext}`,db=createSupabaseAdminClient();const{error:uploadError}=await db.storage.from("product-media").upload(path,await file.arrayBuffer(),{contentType:file.type,upsert:false});if(uploadError)redirect(target(productId,"error","Unable to upload media."));const{error}=await db.from("product_media").insert({product_id:productId,storage_path:path,media_type:file.type==="application/pdf"?"document":"image",mime_type:file.type,file_size:file.size,alt_text:String(form.get("alt_text")??"").slice(0,200)});if(error){await db.storage.from("product-media").remove([path]);redirect(target(productId,"error","Unable to save media details."));}await writeAuditLog({actorId:profile.id,actorRole:profile.role,action:"product.media_added",module:"products",entityType:"product",entityId:productId,description:"Product media added.",newValues:{media_type:file.type==="application/pdf"?"document":"image"}});revalidatePath(`/admin/products/${productId}`);redirect(target(productId,"success","Media uploaded."));}
