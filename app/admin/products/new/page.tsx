import { DashboardShell } from "@/components/dashboard/Shell";import { ProductForm } from "@/components/inventory/ProductForm";import { requirePermission } from "@/lib/auth/permissions";import { getProductOptions } from "@/lib/inventory/products";import { createProductAction } from "../actions";
export default async function NewProductPage({searchParams}:{searchParams:Promise<{error?:string}>}){const{profile,permissions}=await requirePermission("products.create");const[options,message]=await Promise.all([getProductOptions(),searchParams]);return <DashboardShell admin={profile.role==="admin"} employeePermissions={profile.role==="employee"?permissions:undefined} title="Add product" subtitle="Create a simple or variable SEN product.">{message.error?<p className="mb-4 rounded border border-red-200 bg-red-50 p-3 text-red-900">{message.error}</p>:null}<ProductForm action={createProductAction} options={options}/></DashboardShell>}
