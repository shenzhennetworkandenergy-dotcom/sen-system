/* eslint-disable @next/next/no-html-link-for-pages, @next/next/no-img-element */
import{notFound}from"next/navigation";import{DashboardShell}from"@/components/dashboard/Shell";import{ProductForm}from"@/components/inventory/ProductForm";import{ConfirmSubmitButton}from"@/components/ui/ConfirmSubmitButton";import{requirePermission}from"@/lib/auth/permissions";import{getProductOptions}from"@/lib/inventory/products";import{createSupabaseAdminClient}from"@/lib/supabase/admin";import{createVariationAction,deleteProductAction,updateProductAction,uploadProductMediaAction}from"../actions";import{manageProductMediaAction}from"../media-actions";
export const dynamic="force-dynamic";
export default async function ProductDetailPage({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{success?:string;error?:string}>}){const{profile,permissions}=await requirePermission("products.view"),[{id},message]=await Promise.all([params,searchParams]),db=createSupabaseAdminClient();const[{data:product,error},options,{data:assignment},{data:variations},{data:media},{data:revisions},{count:serialCount},{data:balances}]=await Promise.all([db.from("products").select("*").eq("id",id).maybeSingle(),getProductOptions(),db.from("product_category_assignments").select("category_id").eq("product_id",id).eq("is_primary",true).maybeSingle(),db.from("product_variations").select("id,sku,combination_key,status,regular_price,manage_stock").eq("product_id",id).order("created_at"),db.from("product_media").select("id,media_type,media_purpose,visibility,storage_path,alt_text,sort_order,is_primary").eq("product_id",id).order("sort_order"),db.from("product_revisions").select("id,revision_number,actor_id,created_at").eq("product_id",id).order("created_at",{ascending:false}).limit(10),db.from("serial_numbers").select("id",{count:"exact",head:true}).eq("product_id",id),db.from("inventory_balances").select("available,on_hand,warehouses(name,code)").eq("product_id",id)]);if(error)throw new Error("Unable to load product.");if(!product)notFound();const paths=(media??[]).filter(x=>x.media_type==="image").map(x=>x.storage_path),{data:signed}=paths.length?await db.storage.from("product-media").createSignedUrls(paths,3600):{data:[]},urlMap=new Map<string,string>((signed??[]).filter(x=>x.path&&x.signedUrl).map(x=>[String(x.path),String(x.signedUrl)])),formProduct={...product,category_id:assignment?.category_id??"",specifications:JSON.stringify(product.specifications??{},null,2)},update=updateProductAction.bind(null,id),variation=createVariationAction.bind(null,id),upload=uploadProductMediaAction.bind(null,id),manage=manageProductMediaAction.bind(null,id),can=(key:string)=>profile.role==="admin"||permissions.has(key);return <DashboardShell admin={profile.role==="admin"} employeePermissions={profile.role==="employee"?permissions:undefined} title={product.name} subtitle={`SKU ${product.sku} · ${product.product_type} product`}>
 {message.success?<p className="mb-4 rounded border border-green-200 bg-green-50 p-3 text-green-900">{message.success}</p>:null}{message.error?<p className="mb-4 rounded border border-red-200 bg-red-50 p-3 text-red-900">{message.error}</p>:null}
 <section className="mb-6 rounded-2xl border-2 border-blue-200 bg-blue-50/60 p-5"><p className="text-xs font-bold uppercase tracking-wider text-blue-700">Product Operations</p><div className="mt-2 flex flex-wrap items-center justify-between gap-3"><div><strong>{serialCount??0} physical units · {(balances??[]).reduce((s,x)=>s+Number(x.available),0)} available</strong><span className="block text-sm text-[var(--muted-text)]">Model: {product.model_number??"Not provided"}</span></div><div className="flex flex-wrap gap-2">{product.serial_tracking_required&&can("inventory.receive")?<a href={`/admin/products/${id}/stock/add`} className="rounded bg-[var(--primary)] px-4 py-3 font-semibold text-white">Add serialized stock</a>:null}{can("serials.generate")?<a href={`/admin/products/${id}/serials/new`} className="rounded border bg-white px-4 py-3 font-semibold">Generate serials</a>:null}<a href={`/admin/serials?product=${id}`} className="rounded border bg-white px-4 py-3 font-semibold">View serials</a><a href={`/admin/serials/print?product=${id}`} className="rounded border bg-white px-4 py-3 font-semibold">Print labels</a><a href="/admin/serials/scan" className="rounded border bg-white px-4 py-3 font-semibold">Scan serial</a><a href="#product-images" className="rounded border bg-white px-4 py-3 font-semibold">Manage images</a><a href={`/admin/inventory/movements?product=${id}`} className="rounded border bg-white px-4 py-3 font-semibold">Inventory movements</a></div></div></section>
 <section id="product-images" className="mb-6 rounded-xl border bg-[var(--surface)] p-6"><h2 className="text-xl font-semibold">Product Images</h2>{media?.some(x=>x.media_type==="image")?<div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{media.filter(x=>x.media_type==="image").map(item=><article key={item.id} className="overflow-hidden rounded-xl border"><div className="aspect-[4/3] bg-[var(--muted-surface)]">{urlMap.get(item.storage_path)?<img src={urlMap.get(item.storage_path)} alt={item.alt_text??product.name} className="h-full w-full object-contain"/>:null}</div><form action={manage} className="space-y-2 p-3"><input type="hidden" name="media_id" value={item.id}/><b>{item.is_primary?"Main image":"Gallery image"}</b><input name="alt_text" defaultValue={item.alt_text??""} aria-label="Image alt text" className="w-full rounded border p-2"/><div className="flex flex-wrap gap-2"><button name="intent" value="alt" className="rounded border px-2 py-1">Save alt</button>{!item.is_primary?<button name="intent" value="main" className="rounded border px-2 py-1">Set as main</button>:null}<button name="intent" value="remove" className="rounded border border-red-300 px-2 py-1 text-red-700">Remove</button></div></form></article>)}</div>:<p className="mt-3 text-[var(--muted-text)]">No product images uploaded.</p>}
  <form action={upload} className="mt-5 grid gap-3 md:grid-cols-2"><label>New image<input type="file" name="file" required accept="image/jpeg,image/png,image/webp" className="mt-1 block w-full rounded border p-2"/></label><label>Purpose<select name="media_purpose" className="mt-1 w-full rounded border p-2"><option value="gallery_image">Gallery image</option><option value="main_product_image">Main product image</option></select></label><label>Alt text<input name="alt_text" className="mt-1 w-full rounded border p-2"/></label><button className="self-end rounded border px-4 py-2 font-semibold">Upload image</button></form></section>
 <ProductForm action={update} options={options} product={formProduct}/>
 {product.product_type==="variable"?<section className="mt-6 rounded-xl border bg-[var(--surface)] p-6"><h2 className="text-xl font-semibold">Variations</h2>{variations?.length?<div className="mt-4 overflow-x-auto"><table className="w-full min-w-[600px] text-left text-sm"><thead><tr><th>SKU</th><th>Combination</th><th>Price</th><th>Stock level</th><th>Status</th></tr></thead><tbody>{variations.map(item=><tr key={item.id} className="border-t"><td className="py-3">{item.sku}</td><td>{item.combination_key}</td><td>{item.regular_price??"—"}</td><td>{item.manage_stock?"Variation":"Parent"}</td><td>{item.status}</td></tr>)}</tbody></table></div>:<p className="mt-3 text-[var(--muted-text)]">No variations yet.</p>}<form action={variation} className="mt-5 grid gap-3 md:grid-cols-4"><label>Variation SKU<input name="sku" required className="mt-1 w-full rounded border p-2"/></label><label>Combination<input name="combination_key" required className="mt-1 w-full rounded border p-2"/></label><label>Regular price<input name="regular_price" type="number" min="0" step=".0001" className="mt-1 w-full rounded border p-2"/></label><label>Purchase cost<input name="purchase_cost" type="number" min="0" step=".0001" className="mt-1 w-full rounded border p-2"/></label><label className="flex items-center gap-2"><input type="checkbox" name="manage_stock"/>Manage variation stock</label><button className="rounded border px-4 py-2 font-semibold">Add variation</button></form></section>:null}
 <section className="mt-6 rounded-xl border bg-[var(--surface)] p-6"><h2 className="text-xl font-semibold">Product revision history</h2>{revisions?.length?<ol className="mt-3 space-y-2">{revisions.map(r=><li key={r.id} className="rounded border p-3"><strong>Revision {r.revision_number}</strong><span className="block text-sm text-[var(--muted-text)]">{new Date(r.created_at).toLocaleString()}</span></li>)}</ol>:<p className="mt-3 text-[var(--muted-text)]">History begins with the next saved revision.</p>}</section>
 {profile.role==="admin"?<section className="mt-6 rounded-xl border border-red-300 bg-red-50 p-6 text-red-950"><h2 className="text-xl font-semibold">Delete product</h2><p className="mt-2 text-sm">Permanent deletion is allowed only for unused products without stock, serials, variations, movements, reservations, or orders. Products with business history must be archived.</p><form action={deleteProductAction.bind(null,id)}><ConfirmSubmitButton confirmation={`Permanently delete ${product.name}? This cannot be undone.`} className="mt-4 rounded border border-red-700 px-4 py-2 font-semibold text-red-800">Delete unused product</ConfirmSubmitButton></form></section>:null}
 </DashboardShell>}
