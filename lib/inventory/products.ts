import "server-only";
import { createSupabaseAdminClient } from "@/lib/supabase/admin";
import { deriveStockStatus, quantity } from "@/lib/inventory/stock";

export type ProductListParams={q?:string;category?:string;brand?:string;type?:string;stock?:string;status?:string;sort?:string;page?:string};
export async function getProductOptions(){const db=createSupabaseAdminClient();const[{data:categories},{data:brands},{data:attributes}]=await Promise.all([db.from("product_categories").select("id,name,parent_id").eq("is_active",true).order("name"),db.from("brands").select("id,name").eq("is_active",true).order("name"),db.from("attributes").select("id,name").eq("is_active",true).order("sort_order")]);return{categories:categories??[],brands:brands??[],attributes:attributes??[]};}
export async function getProductList(params:ProductListParams){const db=createSupabaseAdminClient();const page=Math.max(1,Number.parseInt(params.page??"1")||1),size=25;let allowedIds:string[]|null=null;if(params.category){const{data}=await db.from("product_category_assignments").select("product_id").eq("category_id",params.category);allowedIds=(data??[]).map(x=>x.product_id);if(!allowedIds.length)return{products:[],count:0,page,size};}let query=db.from("products").select("id,name,slug,sku,product_type,status,brand_id,regular_price,sale_price,currency,stock_status,low_stock_threshold,allow_backorders,updated_at",{count:"exact"});if(params.q)query=query.or(`name.ilike.%${params.q.slice(0,80)}%,sku.ilike.%${params.q.slice(0,80)}%`);if(params.brand)query=query.eq("brand_id",params.brand);if(params.type&&["simple","variable"].includes(params.type))query=query.eq("product_type",params.type);if(params.status&&["draft","active","archived"].includes(params.status))query=query.eq("status",params.status);if(allowedIds)query=query.in("id",allowedIds);const ascending=params.sort==="name";query=query.order(ascending?"name":"updated_at",{ascending}).range((page-1)*size,page*size-1);const{data,error,count}=await query;if(error)throw new Error("Unable to load products.");const ids=(data??[]).map(x=>x.id);const[{data:brands},{data:balances},{data:assignments},{data:categories},{data:media},{data:variations}]=ids.length?await Promise.all([db.from("brands").select("id,name").in("id",[...new Set((data??[]).map(x=>x.brand_id).filter(Boolean))]),db.from("inventory_balances").select("product_id,warehouse_id,available").in("product_id",ids),db.from("product_category_assignments").select("product_id,category_id,is_primary").in("product_id",ids),db.from("product_categories").select("id,name"),db.from("product_media").select("product_id,storage_path,alt_text").in("product_id",ids).eq("is_primary",true),db.from("product_variations").select("product_id").in("product_id",ids).eq("status","active")]):[{data:[]},{data:[]},{data:[]},{data:[]},{data:[]},{data:[]}];const brandMap=new Map((brands??[]).map(x=>[x.id,x.name])),categoryMap=new Map((categories??[]).map(x=>[x.id,x.name]));const products=(data??[]).map(product=>{const available=(balances??[]).filter(x=>x.product_id===product.id).reduce((sum,x)=>sum+quantity(x.available),0);const primary=(assignments??[]).find(x=>x.product_id===product.id&&x.is_primary)??(assignments??[]).find(x=>x.product_id===product.id);return{...product,brand:product.brand_id?brandMap.get(product.brand_id)??null:null,category:primary?categoryMap.get(primary.category_id)??null:null,available,derivedStock:deriveStockStatus(available,quantity(product.low_stock_threshold),product.allow_backorders),image:(media??[]).find(x=>x.product_id===product.id)??null,variationCount:(variations??[]).filter(x=>x.product_id===product.id).length};});return{products:params.stock?products.filter(x=>x.derivedStock===params.stock):products,count:count??0,page,size};}
