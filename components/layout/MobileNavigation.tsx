"use client";import { useEffect,useId,useRef,useState } from "react";import { siteConfig } from "@/config/site";import { Button } from "@/components/ui/Button";
export function MobileNavigation(){const[open,setOpen]=useState(false);const panelId=useId();const panelRef=useRef<HTMLDivElement>(null);useEffect(()=>{if(!open)return;const onKeyDown=(event:KeyboardEvent)=>{if(event.key==="Escape")setOpen(false)};document.addEventListener("keydown",onKeyDown);document.body.style.overflow="hidden";return()=>{document.removeEventListener("keydown",onKeyDown);document.body.style.overflow=""}},[open]);return <div className="lg:hidden"><button type="button" className="inline-flex h-10 items-center justify-center rounded-[var(--radius-md)] border border-[var(--border)] px-3 text-sm font-semibold text-[var(--foreground)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--focus-ring)]" aria-expanded={open} aria-controls={panelId} onClick={()=>setOpen(true)}>Menu</button>{open?<div className="fixed inset-0 z-50 bg-slate-950/40" onMouseDown={(event)=>event.target===event.currentTarget&&setOpen(false)}><div ref={panelRef} id={panelId} role="dialog" aria-modal="true" aria-label="Mobile navigation" className="ml-auto flex h-full w-[min(22rem,88vw)] flex-col bg-[var(--background)] p-6 shadow-2xl"><div className="flex items-center justify-between gap-4"><span className="text-lg font-bold text-[var(--foreground)]">{siteConfig.company.shortName}</span><button type="button" className="rounded-[var(--radius-md)] border border-[var(--border)] px-3 py-2 text-sm font-semibold" onClick={()=>setOpen(false)}>Close</button></div><nav className="mt-8" aria-label="Mobile public navigation"><ul className="space-y-2">{siteConfig.navigation.map((item)=><li key={item.href}><a className="block rounded-[var(--radius-md)] px-3 py-3 text-base font-semibold text-[var(--foreground)] hover:bg-[var(--muted-surface)]" href={item.href} onClick={()=>setOpen(false)}>{item.label}</a></li>)}</ul></nav><div className="mt-auto grid gap-3 pt-8 text-sm"><p className="font-semibold text-[var(--foreground)]">{siteConfig.contact.displayName}</p>{siteConfig.contact.addressLines.map((line)=><p className="text-[var(--muted-text)]" key={line}>{line}</p>)}<a className="font-semibold text-[var(--primary)]" href={siteConfig.contact.telephoneLink}>{siteConfig.contact.phoneDisplay}</a><a className="font-semibold text-[var(--primary)]" href={siteConfig.contact.whatsappLink} target="_blank" rel="noopener noreferrer">Open WhatsApp</a><Button href={siteConfig.publicCtas.requestQuote.href}>{siteConfig.publicCtas.requestQuote.label}</Button></div></div></div>:null}</div>}
