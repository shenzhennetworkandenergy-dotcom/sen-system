import Image from "next/image";
import { siteConfig } from "@/config/site";
import { Container } from "@/components/ui/Container";

export function PublicFooter() { const year = new Date().getFullYear(); return <footer className="border-t border-slate-800 bg-[#07111f] text-white" aria-labelledby="footer-heading"><h2 id="footer-heading" className="sr-only">Footer</h2><Container className="grid gap-10 py-12 lg:grid-cols-[1.2fr_2fr]"><div><Image src={siteConfig.brandAsset.logo} alt={siteConfig.company.logoAlt} width={150} height={115} className="h-16 w-auto object-contain rounded bg-white p-1"/><p className="mt-4 text-base font-semibold">{siteConfig.company.fullName}</p><p className="mt-3 max-w-xl text-sm leading-6 text-slate-300">{siteConfig.description}</p></div><div className="grid gap-8 sm:grid-cols-2 lg:grid-cols-4">{siteConfig.footerGroups.map((group) => <nav key={group.title} aria-labelledby={`footer-${group.title.toLowerCase().replaceAll(" ", "-")}`}><h3 id={`footer-${group.title.toLowerCase().replaceAll(" ", "-")}`} className="text-sm font-semibold">{group.title}</h3><ul className="mt-3 space-y-2 text-sm text-slate-300">{group.links.map((link) => <li key={link.href}><a className="hover:text-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300" href={link.href}>{link.label}</a></li>)}</ul></nav>)}</div></Container><Container className="border-t border-slate-800 py-5 text-sm text-slate-400">© {year} {siteConfig.company.fullName}. Representative brand names are references only, not partnership claims.</Container></footer> }
