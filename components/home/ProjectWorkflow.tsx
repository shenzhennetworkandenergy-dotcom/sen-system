import { workflow } from "@/components/home/homeData";import { Section } from "@/components/ui/Section";import { SectionHeading } from "@/components/ui/SectionHeading";
export function ProjectWorkflow(){return <Section><SectionHeading eyebrow="Workflow" heading="How we support your project" description="A high-level public workflow from requirement discussion to delivery support."/><ol className="mt-10 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{workflow.map((step,i)=><li className="rounded-[var(--radius-lg)] border border-[var(--border)] p-6" key={step}><span className="text-sm font-semibold text-[var(--primary)]">Step {i+1}</span><h3 className="mt-3 text-lg font-semibold text-[var(--foreground)]">{step}</h3></li>)}</ol></Section>}
