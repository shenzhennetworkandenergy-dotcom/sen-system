import { featuredProducts } from "@/components/home/homeData";import { siteConfig } from "@/config/site";import { Button } from "@/components/ui/Button";import { Section } from "@/components/ui/Section";import { SectionHeading } from "@/components/ui/SectionHeading";import { ProductImage } from "@/components/products/ProductImage";
const slug:Record<string,string>={Networking:"networking",Energy:"energy","Medical Equipment":"medical-equipment",Others:"others"};
export function FeaturedProducts(){return <Section className="bg-[var(--muted-surface)]"><SectionHeading eyebrow="Featured technology" heading="Representative sourcing categories with visual fallbacks" description="Product images use Supabase image_url when available and category-specific local fallbacks otherwise."/><div className="mt-10 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{featuredProducts.map(([category,title,description])=><article className="rounded-[var(--radius-lg)] border border-[var(--border)] bg-[var(--surface)] p-5 shadow-sm" key={title}><ProductImage title={title} category={slug[category] ?? "others"}/><p className="mt-5 text-xs font-semibold uppercase tracking-[0.16em] text-[var(--primary)]">{category}</p><h3 className="mt-2 text-lg font-semibold text-[var(--foreground)]">{title}</h3><p className="mt-2 text-sm leading-6 text-[var(--muted-text)]">{description}</p><div className="mt-5 flex flex-wrap gap-3"><Button href={`/products/${slug[category] ?? "others"}`} variant="outline" size="sm">View Category</Button><Button href={siteConfig.publicCtas.requestQuote.href} size="sm">Request Quote</Button></div></article>)}</div></Section>}
