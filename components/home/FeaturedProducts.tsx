import Image from "next/image";
import { getFeaturedProducts } from "@/lib/data/products";
import { siteConfig } from "@/config/site";
import { Button } from "@/components/ui/Button";
import { Section } from "@/components/ui/Section";
import { SectionHeading } from "@/components/ui/SectionHeading";

export async function FeaturedProducts(){const result=await getFeaturedProducts(6);const demo=result.source==="unavailable";return <Section id="featured-products" className="bg-[var(--muted-surface)]"><SectionHeading eyebrow="Featured products" heading="Supabase-backed product highlights" description={result.source==="supabase"?"Published featured products are loaded from Supabase." : result.source==="empty"?"No published featured products are available yet. Add records in Supabase to populate this section." : "Supabase is unavailable in this preview, so clearly marked demo examples are shown."}/>{result.source==="empty"?<div className="mt-10 rounded-2xl border border-dashed border-[var(--border)] bg-[var(--surface)] p-8 text-sm text-[var(--muted-text)]">No live featured products are published yet. The homepage remains ready for database content.</div>:<div className="mt-10 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{result.products.map((product)=><article className="overflow-hidden rounded-2xl border border-[var(--border)] bg-[var(--surface)] shadow-sm" key={product.id}><Image src={product.image_url ?? "/images/home/electronics/control-systems.svg"} alt={product.image_alt ?? `${product.name} product category visual`} width={1600} height={1000} className="aspect-[16/10] w-full object-cover"/><div className="p-5"><div className="flex items-center justify-between gap-3"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[var(--primary)]">{product.category_slug}</p>{demo?<span className="rounded-full bg-amber-100 px-2 py-1 text-xs font-bold text-amber-800">Demo</span>:null}</div><h3 className="mt-2 text-lg font-semibold text-[var(--foreground)]">{product.name}</h3>{product.brand || product.model ? <p className="mt-1 text-xs text-[var(--muted-text)]">{[product.brand, product.model].filter(Boolean).join(" · ")}</p>:null}<p className="mt-3 text-sm leading-6 text-[var(--muted-text)]">{product.short_description}</p><div className="mt-5 flex flex-wrap gap-3"><Button href={`/products#${product.category_slug}`} variant="outline" size="sm">View Product</Button><Button href={siteConfig.publicCtas.requestQuote.href} size="sm">Request Quote</Button></div></div></article>)}</div>}</Section>}
