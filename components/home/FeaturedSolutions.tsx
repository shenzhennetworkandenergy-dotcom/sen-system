import { siteConfig } from "@/config/site";import { Section } from "@/components/ui/Section";import { SectionHeading } from "@/components/ui/SectionHeading";
export function FeaturedSolutions(){return <Section><SectionHeading eyebrow="Solutions" heading="Project-oriented solutions beyond product supply" description="Solution areas are structured for future detail pages while keeping this homepage focused on public-facing capabilities."/><div className="mt-10 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{siteConfig.solutionAreas.map(s=><article className="rounded-[var(--radius-lg)] border border-[var(--border)] p-6" key={s.href}><h3 className="text-lg font-semibold text-[var(--foreground)]">{s.label}</h3><p className="mt-3 text-sm leading-6 text-[var(--muted-text)]">{s.description}</p><p className="mt-4 text-xs font-semibold uppercase tracking-[0.16em] text-[var(--primary)]">Use case</p><p className="mt-1 text-sm text-[var(--muted-text)]">{s.useCase}</p><a href={s.href} className="mt-5 inline-flex text-sm font-semibold text-[var(--primary)]">Explore solution →</a></article>)}</div></Section>}
