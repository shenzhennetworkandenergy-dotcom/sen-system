import { siteConfig } from "@/config/site";import { Button } from "@/components/ui/Button";import { Section } from "@/components/ui/Section";
export function FinalCta(){return <Section><div className="rounded-[var(--radius-lg)] bg-[var(--foreground)] p-8 text-[var(--background)] sm:p-12"><h2 className="text-3xl font-semibold tracking-tight sm:text-4xl">Planning a technology or infrastructure project?</h2><p className="mt-4 max-w-2xl text-base leading-7 opacity-80">Share your requirements with the SEN team for sourcing, quotation and delivery support.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Button href={siteConfig.publicCtas.requestQuote.href} size="lg">Request a Quote</Button><Button href={siteConfig.publicCtas.contact.href} variant="secondary" size="lg">Contact SEN</Button></div></div></Section>}
