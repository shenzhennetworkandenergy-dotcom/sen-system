import { industries } from "@/components/home/homeData";import { Section } from "@/components/ui/Section";import { SectionHeading } from "@/components/ui/SectionHeading";
export function IndustriesSection(){return <Section className="bg-[var(--muted-surface)]"><SectionHeading eyebrow="Industries" heading="Industries we serve" description="The homepage keeps industry content structured so future pages can add deeper solution and product mapping."/><div className="mt-8 flex flex-wrap gap-3">{industries.map(i=><span className="rounded-full border border-[var(--border)] bg-[var(--surface)] px-4 py-2 text-sm font-medium text-[var(--foreground)]" key={i}>{i}</span>)}</div></Section>}
