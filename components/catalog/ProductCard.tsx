/* eslint-disable @next/next/no-img-element */
import type { AwaitedReturn } from "@/types/catalog";

export function ProductCard({ product }: { product: AwaitedReturn }) {
  const price = product.sale_price ?? product.regular_price;
  return <article className="sen-catalogue-card group"><a href={`/products/${product.slug}`} className="block"><div className="relative aspect-[4/3] overflow-hidden rounded-2xl bg-[radial-gradient(circle_at_50%_35%,white,#eaf1ff)]">{product.imageUrl ? <img src={product.imageUrl} alt={product.imageAlt} className="h-full w-full object-contain p-4 transition duration-700 group-hover:scale-110"/> : <div className="grid h-full place-items-center text-sm text-slate-500">Product image coming soon</div>}<span className="absolute left-3 top-3 rounded-full bg-[#07102f]/90 px-3 py-1 text-xs font-semibold text-cyan-200">{product.sen_business_category}</span></div><div className="p-1 pt-5"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-cyan-700">{product.brand ?? "SEN sourced"}</p><h2 className="mt-2 text-xl font-semibold text-[#10152f] transition group-hover:text-[#245fc8]">{product.name}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-slate-600">{product.short_description ?? "Enterprise-grade equipment sourced and supported by SEN."}</p><div className="mt-5 flex items-end justify-between gap-3"><div>{price ? <><span className="text-xs text-slate-500">Starting from</span><strong className="block text-xl text-[#10152f]">{new Intl.NumberFormat("en", { style: "currency", currency: product.currency, maximumFractionDigits: 0 }).format(price)}</strong></> : <strong className="text-base text-[#10152f]">Request quotation</strong>}</div><span className={`rounded-full px-3 py-1 text-xs font-semibold ${product.available > 0 ? "bg-emerald-50 text-emerald-800" : "bg-slate-100 text-slate-700"}`}>{product.available > 0 ? "Available" : "Contact for availability"}</span></div><span className="mt-5 inline-flex font-semibold text-[#245fc8]">View full details <span className="ml-2 transition-transform group-hover:translate-x-1">→</span></span></div></a></article>;
}
