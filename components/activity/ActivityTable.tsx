import { activityLabel, formatActivityTime, safeJsonSummary } from "@/lib/audit/format";

export type ActivityRow = { id: number; actor_id: string | null; action: string; module: string | null; entity_type: string | null; entity_id: string | null; description: string | null; old_values: unknown; new_values: unknown; created_at: string };

export function ActivityTable({ rows, people = {} }: { rows: ActivityRow[]; people?: Record<string, { name: string; email: string | null }> }) {
  if (!rows.length) return <p className="rounded-xl border bg-[var(--surface)] p-6 text-[var(--muted-text)]">No matching activity has been recorded.</p>;
  return <div className="overflow-x-auto rounded-xl border bg-[var(--surface)]"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-3">Date and time</th><th>User</th><th>Action</th><th>Module</th><th>Description</th><th>Details</th></tr></thead><tbody>{rows.map((row) => { const oldSummary=safeJsonSummary(row.old_values); const newSummary=safeJsonSummary(row.new_values); return <tr key={row.id} className="border-b align-top"><td className="whitespace-nowrap p-3">{formatActivityTime(row.created_at)}</td><td>{row.actor_id && people[row.actor_id] ? <a href={`/admin/activity/${row.actor_id}`} className="font-semibold">{people[row.actor_id].name}</a> : "System"}</td><td className="font-medium">{activityLabel(row.action)}</td><td>{row.module ?? "—"}</td><td className="max-w-sm">{row.description ?? "—"}{row.entity_type ? <span className="block text-xs text-[var(--muted-text)]">{row.entity_type}{row.entity_id ? ` · ${row.entity_id}` : ""}</span> : null}</td><td>{oldSummary || newSummary ? <details><summary className="cursor-pointer font-semibold">Summary</summary><p className="mt-1 max-w-sm text-xs text-[var(--muted-text)]">{oldSummary ? `Before: ${oldSummary}` : ""}{oldSummary && newSummary ? " · " : ""}{newSummary ? `After: ${newSummary}` : ""}</p></details> : "—"}</td></tr>; })}</tbody></table></div>;
}
